// convert to decimal 2 

#include<iostream>
using namespace std;

int main()
{
    int t; cin >> t;

    while(t--) {
        int n, cal = 0;
        long long sum = 0;
        cin >> n;
        while(n > 0) {
            if (n)
        }
    }
}